---
description: Asset Center → Asset types
---

# Asset types

Path: **Asset Center → Asset types**.

Categories for assets. Choose a type when creating an asset. Delete or reassign assets before deleting a type that still has members.
