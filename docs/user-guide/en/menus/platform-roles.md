---
description: Platform Administration → Roles (super admin only)
---

# Roles

Path: **Platform Administration → Roles**. **Super admin only**.

Platform-scoped roles with per-module permissions. System/protected roles cannot be deleted. Workspace business permissions can be shown for admin-style roles.
