---
description: MQTT 接入 → 消息监控
---

# 消息监控

路径：**MQTT 接入 → 消息监控**。

联调时查看 Topic、QoS、是否解析成功、载荷预览（敏感信息会脱敏）。用于确认网关是否在发、Worker 是否在收。

本页只显示 **最近约 500 条（约 1 小时）**。解析成功的报文更新信标/网关后 **不入库**；解析失败会在库里留几天，方便排障。Worker 或 Redis 重启后列表会空，有新上报即恢复。
