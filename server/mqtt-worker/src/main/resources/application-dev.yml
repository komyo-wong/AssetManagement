spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/asset_management}
    username: ${DB_USERNAME:asset_app}
    password: ${DB_PASSWORD}

logging:
  level:
    com.assetmanagement: DEBUG
